<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cms549.connectfour">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AccountScreen"></activity>
        <activity android:name=".AchievementsScreen"></activity>
        <activity android:name=".CreateAccountScreen"></activity>
        <activity android:name=".ChooseChipScreen"></activity>
        <activity android:name=".ChooseLevelScreen"></activity>
        <activity android:name=".GameScreen"></activity>
    </application>

</manifest>